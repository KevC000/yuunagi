name: yuunagi
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  go_router: ^10.0.0
  jm_dict: ^0.2.0
  kanji_dictionary: ^1.0.5+1
  flutter:
    sdk: flutter
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/raw/jmdict.json
    - assets/raw/kanjidic2.json
